export class WordRateItem {

  name = '';
  count = '';
  headImg = '';

  constructor(options) {
    this.name = options.name;
    this.count = options.count;
    this.headImg = options.headImg;
  }
}
