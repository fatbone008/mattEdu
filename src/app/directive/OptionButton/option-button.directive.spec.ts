import { OptionButtonDirective } from './option-button.directive';

describe('OptionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
